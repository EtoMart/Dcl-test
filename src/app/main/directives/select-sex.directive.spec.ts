import { SelectSexDirective } from './select-sex.directive';

describe('SelectSexDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectSexDirective();
    expect(directive).toBeTruthy();
  });
});
